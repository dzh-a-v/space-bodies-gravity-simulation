# План создания проекта симулятора гравитационных систем

## Summary

Создать Python-проект в `program/`, строго ориентированный на ТЗ из `docs/tz/tz.tex`: ньютоновская гравитация, интегратор Верле, столкновения, фрагментация, предел Роша, CSV-сценарии, GUI с таблицей и тремя 2D-проекциями `XY`, `XZ`, `YZ`.

Основной стек: `Python`, `PySide6`, `pyqtgraph`, `numpy`, `pytest`. Первая версия делает упор на физическую корректность и тестируемость; оптимизация под большие системы закладывается архитектурно, но не усложняет v1.

## Иерархия проекта

```text
program/
├─ pyproject.toml
├─ README.md
├─ src/
│  └─ gravity_sim/
│     ├─ __init__.py
│     ├─ main.py
│     ├─ core/
│     │  ├─ constants.py
│     │  ├─ vector.py
│     │  ├─ body.py
│     │  ├─ system_state.py
│     │  └─ validation.py
│     ├─ physics/
│     │  ├─ gravity.py
│     │  ├─ integrators.py
│     │  ├─ collisions.py
│     │  ├─ fragmentation.py
│     │  ├─ roche.py
│     │  └─ engine.py
│     ├─ io/
│     │  ├─ csv_schema.py
│     │  ├─ csv_loader.py
│     │  ├─ csv_saver.py
│     │  └─ presets.py
│     ├─ ui/
│     │  ├─ app.py
│     │  ├─ main_window.py
│     │  ├─ projection_view.py
│     │  ├─ body_table_model.py
│     │  ├─ controls_panel.py
│     │  └─ dialogs.py
│     └─ resources/
│        └─ presets/
│           ├─ empty.csv
│           ├─ earth_moon.csv
│           └─ simple_collision.csv
└─ tests/
   ├─ test_validation.py
   ├─ test_csv_io.py
   ├─ test_gravity.py
   ├─ test_verlet.py
   ├─ test_collisions.py
   ├─ test_fragmentation.py
   └─ test_roche.py
```

## Key Changes / Implementation Plan

1. Подготовить каркас проекта: `pyproject.toml`, пакет `gravity_sim`, точку входа `main.py`, зависимости `numpy`, `PySide6`, `pyqtgraph`, `pytest`.
2. Реализовать доменную модель: `Body`, `SystemState`, настройки симуляции и валидацию ограничений из ТЗ.
3. Реализовать CSV-сценарии с колонками `name,mass,radius,x,y,z,vx,vy,vz,ax,ay,az`.
4. Реализовать физическое ядро: ньютоновская гравитация, Velocity Verlet, прямой solver `O(n²)` с возможностью будущей замены.
5. Реализовать столкновения по правилам ТЗ: слияние, вероятностное разрушение, сравнение `v_rel` и `v_esc`.
6. Реализовать фрагментацию с лимитами массы, числа фрагментов, общего числа объектов и запретом повторной фрагментации.
7. Реализовать предел Роша с накоплением 24 часов внутри зоны.
8. Реализовать GUI: таблица, настройки, три 2D-проекции, запуск/пауза/сброс, загрузка/сохранение CSV.

## Test Plan

- Unit-тесты валидации диапазонов массы, радиуса, уникальности имён и числа фрагментов.
- Unit-тесты CSV: успешная загрузка/сохранение, отсутствующие колонки, дубликаты имён, некорректные числа.
- Unit-тесты гравитации: направление силы, симметрия парного взаимодействия, отсутствие деления на ноль.
- Unit-тесты Velocity Verlet на простой двухтельной или однотельной задаче.
- Unit-тесты столкновений: слияние при малом отношении масс, фрагментация при высокой относительной скорости, слияние при низкой скорости.
- Unit-тесты предела Роша: накопление времени внутри зоны, сброс накопления вне зоны, разрушение после 24 часов.
- Ручные сценарии проверки GUI: загрузка `earth_moon.csv`, пауза/старт, изменение скорости, сохранение CSV, отображение всех трёх проекций.

## Assumptions

- Используется папка `program/`, потому что она явно указана пользователем и уже существует.
- Дополнительные улучшения допустимы только как внутренняя архитектурная подготовка и не должны менять пользовательский функционал из ТЗ.
- 3D-рендеринг, drag-and-drop, вращение тел вокруг оси, релятивистские эффекты, магнитные поля и повторная фрагментация осколков не реализуются.
